---

codecov:
  require_ci_to_pass: true
  notify:
    wait_for_ci: true
    after_n_builds: 1

parsers:
  javascript:
    enable_partials: yes

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
